# Project Rules & Guidelines

## Deployment Process for Orac Server
When deploying Orac Server:
1. Run `export_for_distrib.py` to package the server structure, clean development artifacts, ignore local configs/tokens, and generate start scripts.
2. Push the generated package to the distribution repository.
